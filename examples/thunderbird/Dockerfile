# GENERATED by bin/generate-viewer from apps/thunderbird.conf + templates/ -- DO NOT EDIT.
FROM ghcr.io/modernmavericks/porthole-base:20260803.9

# Linux Thunderbird's own package(s), from the vendor's apt repo -- installed here on YOUR Mac, so Porthole
# never redistributes vendor software. The common runtime (xpra + Xvfb, dbus, keyring, socat, the
# menu daemon, the Mac-font alias) is already in the pre-built shared base ghcr.io/modernmavericks/porthole-base:20260803.9.
RUN apt-get update \
    && apt-get install -y --no-install-recommends thunderbird \
    && rm -rf /var/lib/apt/lists/*

# The app refuses to run as root; give it a real user (Electron apps and 1Password
# alike). USER_GROUP pins an existing group the app's own package created.
RUN useradd -m -u 1000 thunderbird

COPY start-thunderbird-gui.sh thunderbird-child.sh /usr/local/bin/
RUN chmod 755 /usr/local/bin/start-thunderbird-gui.sh /usr/local/bin/thunderbird-child.sh
ENTRYPOINT ["/usr/local/bin/start-thunderbird-gui.sh"]
